stages:
  - deploy

deploy_production:
  stage: deploy
  tags:
    - geco-servidor-produccion
  script:
    - echo "Deploy en servidor..."
    - cd /var/www/GECOProatam
    - git fetch origin
    - git reset --hard origin/main
    - bash deploy.sh